# GHL Email Templates

A collection of HTML email templates for use with GoHighLevel (GHL).

Each template file is tracked in [AGENTS.md](./AGENTS.md) with its GitHub raw URL for direct use with GHL's MCP server tools (e.g., `ghl_emails_create-template` via `importURL`).
